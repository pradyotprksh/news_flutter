/// A set of enums use all over the

/// Type of message.
enum MessageType {
  error,
  success,
  information,
}

/// Page status
enum PageStatus {
  loading,
  idle,
  error,
  success,
  offline,
  online,
}
